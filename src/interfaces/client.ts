export enum ClientType {
  ChatGPT = 'chatgpt',
  TurboGPT = 'gpt3.5',
}

export enum TriggerMode {
  Problem = 'problem',
  Challenge = 'challenge',
}

export enum AssistanceMode {
  Timer = 'timer',
  Manual = 'manual',
}
